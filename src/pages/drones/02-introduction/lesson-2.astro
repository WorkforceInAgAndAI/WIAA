---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Sidebar from '../../../components/Sidebar.astro';
import LessonNav from '../../../components/LessonNav.astro';

const content = await import('../../../content/drones/02-introduction/lesson-2.md');
const { Content } = content;
const { title, chapter, lesson } = content.frontmatter;
---

<BaseLayout title={title}>
  <div class="container" style="max-width: 1400px; margin: 30px auto; padding: 0 20px;">
    <div style="display: grid; grid-template-columns: 250px 1fr; gap: 24px; align-items: start;">
      <Sidebar currentChapter={chapter} currentLesson={lesson} />
      <div class="content-area">
        <div class="content-header">
  <div class="page-header-bar">
    <div class="tab-buttons">
      <a href="/WIAA/drones" class="tab-btn active">Learning Module</a>
      <a href="/WIAA/drones/instructor" class="tab-btn">Instructor Resources</a>
    </div>
  </div>
  <p class="chapter-label">Chapter {chapter}: Introduction to drones</p>
          <h1>{title}</h1>
        </div>
        <article class="lesson-content">
          <Content />
        </article>
        <LessonNav 
          prev={{ href: '/drones/02-introduction/lesson-1', label: 'Previous' }}
          next={{ href: '/drones/02-introduction/review', label: 'Next' }}
          progress={`${chapter}.${lesson} / 7 Chapters`}
        />
      </div>
    </div>
  </div>

</BaseLayout>
<style is:global>
  .lesson-content img {
    max-width: 800px !important;
    height: auto !important;
    display: block !important;
    margin: 20px auto !important;
  }

  @media (max-width: 767px) {
    .lesson-content img {
      max-width: 280px !important;
    }
.page-header-bar {
    display: none;
  }

  @media (max-width: 767px) {
    .page-header-bar {
      display: block;
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 2px solid var(--isu-gold);
    }

    .tab-buttons {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .tab-btn {
      padding: 12px 20px;
      background: #f5f5f5;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
      transition: all 0.2s;
      text-align: center;
      text-decoration: none;
      color: var(--text-medium);
      font-size: 0.95em;
    }

    .tab-btn:hover {
      background: #e8e8e8;
    }

    .tab-btn.active {
      background: var(--isu-red);
      color: white;
    }
  }
/* Add this CSS to your lesson 2.2 page <style is:global> block */

/* Lesson intro - reuse from previous lesson */
.lesson-intro {
  background: linear-gradient(135deg, #f5f5f5 0%, #ffffff 100%);
  padding: 20px;
  border-left: 4px solid var(--isu-red);
  border-radius: 4px;
  margin-bottom: 30px;
  font-size: 1.1em;
  line-height: 1.8;
  color: var(--text-dark);
}

/* Section intro text */
.section-intro {
  font-size: 1.05em;
  color: var(--text-medium);
  font-style: italic;
  margin-bottom: 24px;
  padding: 0 12px;
}

/* Applications Grid - 6 cards */
.applications-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
  margin-bottom: 35px;
}

.application-card {
  background: white;
  border: 1px solid var(--border-light);
  border-radius: 8px;
  padding: 24px;
  text-align: center;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  transition: all 0.3s ease;
}

.application-card:hover {
  box-shadow: 0 4px 16px rgba(196, 30, 58, 0.12);
  border-color: var(--isu-red);
  transform: translateY(-4px);
}

.card-icon {
  font-size: 2.5em;
  margin-bottom: 12px;
}

.application-card h4 {
  color: var(--isu-red);
  margin-top: 0;
  margin-bottom: 12px;
  font-size: 1.1em;
}

.application-card p {
  color: var(--text-medium);
  line-height: 1.6;
  margin: 0;
  font-size: 0.95em;
}

/* Task Comparison Section */
.task-comparison {
  display: grid;
  grid-template-columns: 1fr;
  gap: 24px;
  margin-bottom: 35px;
  background: white;
  padding: 24px;
  border-radius: 8px;
  border: 1px solid var(--border-light);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
}

.task-column {
  flex: 1;
}

.column-header {
  font-weight: 700;
  font-size: 1.05em;
  padding: 12px 16px;
  border-radius: 6px;
  margin-bottom: 16px;
  text-align: center;
}

.column-header.manual {
  background: linear-gradient(135deg, rgba(255, 184, 28, 0.15) 0%, rgba(255, 184, 28, 0.1) 100%);
  color: var(--isu-red);
  border: 1px solid var(--isu-gold);
}

.column-header.drone {
  background: linear-gradient(135deg, rgba(196, 30, 58, 0.15) 0%, rgba(196, 30, 58, 0.1) 100%);
  color: var(--isu-red);
  border: 1px solid var(--isu-red);
}

.task-list {
  list-style-position: inside;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.task-list li {
  color: var(--text-medium);
  line-height: 1.6;
  padding-left: 8px;
}

/* Learning Activities */
.activities-box {
  background: linear-gradient(135deg, rgba(196, 30, 58, 0.08) 0%, rgba(255, 184, 28, 0.08) 100%);
  border-left: 5px solid var(--isu-red);
  border-radius: 6px;
  padding: 20px;
  margin-bottom: 20px;
}

.activities-box h4 {
  color: var(--isu-red);
  margin-top: 0;
  margin-bottom: 10px;
  font-size: 1.05em;
}

.activities-box p {
  color: var(--text-dark);
  line-height: 1.6;
  margin: 0;
}

/* Responsive - Tablet and larger */
@media (min-width: 768px) {
  .lesson-intro {
    padding: 24px;
    font-size: 1.15em;
  }

  .applications-grid {
    grid-template-columns: 1fr 1fr;
    gap: 24px;
  }

  .task-comparison {
    grid-template-columns: 1fr 1fr;
    gap: 32px;
    padding: 32px;
  }

  .application-card {
    padding: 28px;
  }
}

/* Responsive - Desktop */
@media (min-width: 969px) {
  .applications-grid {
    grid-template-columns: 1fr 1fr 1fr;
    gap: 28px;
  }

  .application-card {
    padding: 32px;
  }

  .task-comparison {
    padding: 40px;
  }
}
  }
</style>
<style>
  @media (max-width: 768px) {
    div[style*="grid-template-columns"] {
      display: flex !important;
      flex-direction: column-reverse !important;
      gap: 16px !important;
    }
  }
</style>