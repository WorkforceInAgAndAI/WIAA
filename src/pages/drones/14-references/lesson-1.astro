---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Sidebar from '../../../components/Sidebar.astro';
import LessonNav from '../../../components/LessonNav.astro';
const content = await import('../../../content/drones/14-references/lesson-1.md');
const { Content } = content;
const { title, chapter, lesson } = content.frontmatter;
---
<BaseLayout title={title}>
  <div class="container" style="max-width: 1400px; margin: 30px auto; padding: 0 20px;">
    <div style="display: grid; grid-template-columns: 250px 1fr; gap: 24px; align-items: start;">
      <Sidebar currentChapter={chapter} currentLesson={lesson} />
      <div class="content-area">
        <div class="content-header">
          <h1>{title}</h1>
        </div>
        <article class="lesson-content">
          <Content />
        </article>
        <LessonNav
          prev={{ href: '/drones/12-careers/review', label: 'Previous' }}
          progress={`${chapter}.${lesson} / 12 Chapters`}
        />
      </div>
    </div>
  </div>
</BaseLayout>
<style is:global>
  :root {
    --isu-red: #c41e3a;
    --isu-gold: #f8c600;
    --border-light: #ddd;
    --text-dark: #1a1a1a;
    --text-medium: #666;
    --isu-dark-red: #a01729;
  }
  .references-list {
    padding-left: 26px;
    color: var(--text-dark);
    line-height: 1.9;
  }
  .references-list li {
    margin-bottom: 14px;
    font-size: 14px;
  }
  .references-list a {
    color: var(--isu-red);
    word-break: break-word;
  }
  .references-list a:hover {
    color: var(--isu-dark-red);
  }
  .references-list em {
    font-style: italic;
    color: var(--text-medium);
  }
</style>
<style>
  @media (max-width: 768px) {
    div[style*="grid-template-columns"] {
      display: flex !important;
      flex-direction: column-reverse !important;
      gap: 16px !important;
    }
  }
</style>