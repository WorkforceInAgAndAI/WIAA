---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import SatSidebar from '../../../components/SatSidebar.astro';
import SatLessonNav from '../../../components/SatLessonNav.astro';

const content = await import('../../../content/satellites/09-imagery-pipeline/lesson-1.md');
const { Content } = content;
const { title, chapter, lesson } = content.frontmatter;
---

<BaseLayout title={title}>
  <div class="container" style="max-width: 1400px; margin: 30px auto; padding: 0 20px;">
    <div style="display: grid; grid-template-columns: 250px 1fr; gap: 24px; align-items: start;">
      <SatSidebar currentChapter={chapter} currentLesson={lesson} />
      <div class="content-area">
        <div class="content-header">
          <p class="chapter-label">Chapter {chapter}: Satellite Imagery Processing Pipeline</p>
          <h1>{title}</h1>
        </div>
        <article class="lesson-content">
          <Content />
        </article>
        <SatLessonNav
          prev={{ href: '/satellites/08-ai-agriculture/review', label: 'Previous' }}
          next={{ href: '/satellites/09-imagery-pipeline/lesson-2', label: 'Next' }}
          progress={`${chapter}.${lesson} / 11 Chapters`}
        />
      </div>
    </div>
  </div>
</BaseLayout>
<style is:global>
  :root {
    --isu-red: #c41e3a;
    --isu-gold: #f8c600;
    --border-light: #ddd;
    --text-dark: #1a1a1a;
    --text-medium: #666;
    --isu-dark-red: #a01729;
  }
  .lesson-content p {
    line-height: 1.8;
    color: var(--text-dark);
    margin-bottom: 18px;
  }
  .lesson-intro {
    background: linear-gradient(135deg, #f5f5f5 0%, #ffffff 100%);
    padding: 20px;
    border-left: 4px solid var(--isu-red);
    border-radius: 4px;
    margin-bottom: 30px;
    font-size: 1.1em;
    line-height: 1.8;
    color: var(--text-dark);
  }
  .benefits-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 30px;
  }
  .benefit-card {
    background: white;
    border: 1px solid var(--border-light);
    border-radius: 8px;
    padding: 18px;
    padding-left: 70px;
    position: relative;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  }
  .benefit-number {
    position: absolute;
    left: 18px;
    top: 18px;
    width: 34px;
    height: 34px;
    background: linear-gradient(135deg, var(--isu-red), var(--isu-dark-red));
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.1em;
  }
  .benefit-card h4 {
    color: var(--isu-red);
    margin: 0;
    font-size: 1em;
  }
  .shiny-cta {
    background: linear-gradient(135deg, #1a1a2e, #2d2d4a);
    border-radius: 12px;
    padding: 28px;
    margin: 28px 0;
    text-align: center;
  }
  .shiny-cta h4 {
    color: white;
    margin: 0 0 8px;
    font-size: 1.2em;
  }
  .shiny-cta p {
    color: #cbd5e1;
    margin: 0 0 18px;
    line-height: 1.6;
  }
  .shiny-cta__button {
    display: inline-block;
    background: var(--isu-gold);
    color: #1a1a2e;
    font-weight: 700;
    padding: 12px 28px;
    border-radius: 8px;
    text-decoration: none;
  }
  @media (min-width: 768px) {
    .benefits-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>
<style>
  @media (max-width: 768px) {
    div[style*="grid-template-columns"] {
      display: flex !important;
      flex-direction: column-reverse !important;
      gap: 16px !important;
    }
  }
</style>